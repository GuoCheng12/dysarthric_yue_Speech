# Artifact Registry

This directory tracks generated model and evaluation artifacts.

Do not commit model weights, full checkpoints, optimizer states, or per-utterance private predictions.

Use `artifacts/registry/` for small metadata records.
