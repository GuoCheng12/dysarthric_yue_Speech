# Cleanup record — 2026-09-04

The repository was reorganized around the therapist-inspired closed-loop adaptation
framework. Before deleting untracked research code, a repository-external recovery
snapshot was created without `.git`, `.secrets`, caches, model weights, or audio.

- archive: `/data/qwen3-asr/archives/dysarthric_yue_cleanup_20260904/repository_code_and_records_before_cleanup.tar.gz`
- archive SHA-256: `3bc87277e0d94b8a1ac41dbf544a26426a0e8287db4713352472f3d23c3f4d2d`
- tracked-change patch: `/data/qwen3-asr/archives/dysarthric_yue_cleanup_20260904/tracked_changes.patch`
- patch SHA-256: `c968eda45bc94067eca4d96bd0e44e49819f4bec459600048cafd858fd2735b6`

Removed execution lines are summarized in `records/rejected_approaches.md`. Original
patient data, Qwen/CosyVoice weights, checkpoints, private Setting D manifests, and the
local `.secrets` directory were not removed or copied into the archive.

The removed code includes the old `SkillOpt`, `probes`, and `inference/multi_agent`
trees; one-off Setting C builders and leakage scripts; Matcha-TTS and DSI patient-model
branches; Meta-Utility Gate/verifier experiments; and obsolete ASR CTC, bridge,
hard-weighting, preference, and sweep runners. Sanitized aggregate results that still
motivate the current method remain under `records/`.

The maintained runtime is now limited to:

- `src/therapist_harness/`: schemas, evidence, proposal materialization, Agent boundary,
  and immutable event ledger;
- `finetune/scripts/`: one LoRA trainer, one strict ASR evaluator, and registry checks;
- `synthesis/cosyvoice3_patient_sft/`: the frozen Speech Generator inference boundary;
- `configs/`, `data/registry/`, and `artifacts/registry/`: experiment constants and
  artifact identities.
