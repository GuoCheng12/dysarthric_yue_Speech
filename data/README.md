# Data Registry

This directory tracks dataset versions and split definitions.

Do not commit raw patient audio, raw private transcripts, or per-utterance private prediction tables.

Use `data/registry/` for small metadata files that describe private datasets and how to verify them.
